import * as path from "path";

import { Collection } from "discord.js";



class EventLoader {
  // private events: Collection<string, { filePath: string; execute: Function }>;

  private folderPath: string = path.join(__dirname, "../../events");

  
}

const { SuwaClient } = require("../client/bot");

class PrefixCommand {
  /**
   *
   * @param {SuwaClient} client
   */
  constructor(client) {
    this.client = client;
  }

  async execute() {}
}
